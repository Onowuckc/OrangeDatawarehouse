# Example environment variables (DO NOT COMMIT secrets)
# Department passwords (example)
DEPT_PASS_FIN=fin-secret
DEPT_PASS_SAL=sales-secret
DEPT_PASS_MKT=marketing-secret

# Database URL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db/dwdb

# JWT secret (use strong random in production)
JWT_SECRET=replace-me-with-random-secret

# Redis URL for Celery (if used)
REDIS_URL=redis://localhost:6379/0
